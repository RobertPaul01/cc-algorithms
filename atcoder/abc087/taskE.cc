#include "bits/stdc++.h"
#define _ ios_base::sync_with_stdio(0);cin.tie(0);

using namespace std;

int main() { _
    int n, m, s, t;
    cin >> n >> n >> m >> s >> t;
    for (int i = 0; i < m; i++) {
        int u, v, d;
        cin >> u >> v >> d;
        
    }
    return 0;
}
