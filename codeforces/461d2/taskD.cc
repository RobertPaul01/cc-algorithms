#include "bits/stdc++.h"

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<string> strs;
    for (int i = 0; i < n; i++) {
        string x;
        cin >> x;
        strs.push_back(x);
    }
    return 0;
}
